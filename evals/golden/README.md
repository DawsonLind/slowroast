# Golden Set

7 hand-labeled URLs used for regression. See `docs/architecture.md` §6.

Each JSON file in this directory has:
- `url`: the URL to analyze
- `expectedFindings`: map of category → minimum findings count
- `expectedTopPriority`: one of the expected findings' IDs
- `notes`: freeform commentary, known edge cases
